---
name: animal-io-web-pivot
description: ⚠️ PIVOT 27 mai — lupul/animal-io PUS PE RAFT (prea complex); focus pe un joc MIC simplu single-player (shooting/arcade/match-3). Istoricul lupului mai jos = context, NU direcția activă
metadata: 
  node_type: memory
  type: project
  originSessionId: 08f0c08d-bdd8-418c-9003-781403a86d19
---

## 🔄🔄 PIVOT 28 mai 2026 (DUPĂ pilot Meshy reușit) — joc PLATFORMER cu skilluri (NU mai shooter)
**Userul a decis în final de sesiune:** „în loc de shot cu atacatori, vreau să facem acest joc unul de logică, cu obstacole, skilluri doar cu un singur player". Gen ales: **platformer 3D cu skilluri** (gen Hollow Knight 3D / Crash light) — NU puzzle, NU stealth, NU mix. Single player. Arcașul Meshy = personajul (animații Idle/Walk perfecte; Archery_Shot ignorat). Harta de 10 camere reutilizată ca niveluri progresive.

✅✅ MILESTONE 28 mai — PROTOTIP PLATFORMER funcțional în `Assets/Scenes/Shooter.unity`:
- `PlatformerController.cs` (move WASD + jump SPACE cu coyote 0.12s + jump buffer 0.12s + low-jump multiplier + dash Shift cu cooldown 0.8s + respawn Q + IsGrounded via raycast + airControl 0.55 + faces moveDir). Folosește `UnityEngine.InputSystem` (proiectul are noul Input System). `DashCooldownNormalized` expus pentru HUD.
- `Spikes.cs` — OnTriggerEnter/OnCollisionEnter pe root tag „Player" → `pc.Respawn()`. Funcționează cu BoxCollider isTrigger=true (player trece prin trigger → moare).
- `GoalZoneTrigger.cs` + `RoomController.SpawnGoal()` — fiecare cameră spawnează cilindru galben (boss room mov) ca trigger. Player intră → `room.OnGoalReached()` → `GameManager.OnRoomCleared()` → next room. Înlocuiește mecanica „omoară toți inamicii".
- `GameManager.cs` rescris simplu: doar `RoomsCleared/TotalRooms` + Dash cooldown bar + Q respawn + R restart la win. Scos HP bar, boss bar, choosing upgrade, OnPlayerDeath logic. Păstrate stub-uri `OnEnemyKilled()` + `OnPlayerDeath()` ca HeroController.cs vechi să compileze.
- `MapBuilder.cs` — scoasă crearea BossGate (porțile aurii). Camere liber accesibile între ele. Funcția `BuildBossGate` rămâne în fișier dar nu mai e apelată.
- **Player swap-uit** în Shooter.unity: scos `HeroController` + `BowVisual` componente; adăugat `PlatformerController`. MeshyArcher rămâne ca child (Idle/Walk se redau prin Animator existent). Player Rigidbody freeze rotation + interpolate.
- **Demo nivel 1** = parent „Demo_Level1" cu 2 obstacole în camera 0 (în jurul Player la `(-1, 1, 0)`):
  - `SpikeBar_Jump` la (2.5, 0.4, 1.5), scale (3, 0.8, 0.3), roșu, isTrigger → forțează SPACE jump
  - `SpikeBar_Dash` la (4.5, 0.4, 3.5), scale (3.5, 0.8, 1.2), roșu, isTrigger → forțează Shift dash
  - GoalZone (galben, spawnat de RoomController) la (5.5, 0.6, 5.5)
- Scena salvată. Userul testează Play manual (MCP play crapă editor — lecție veche).

**LECȚII tehnice noi (28 mai, sesiune asta):**
- Adăugare component custom prin MCP după ce fișierul a fost scris: trebuie `manage_asset action=import path=...` explicit ÎNTÂI ca Unity să-l proceseze (genereze .meta + compile + register type). Refresh_unity singur NU declanșează indexarea noului tip — `manage_components add` va eșua cu „Component type 'X' not found" până asset-ul e importat. Verifică cu `unity_reflect search query=...` ca să confirmi tipul e cunoscut înainte de add.
- `manage_components remove/add` NU merge în play mode. Stop play întâi.
- `manage_scene save` NU merge în play mode. Stop play întâi.
- Editorul re-intră în play mode SINGUR des → check-uire `manage_editor stop` pre-emptiv înainte de operații care nu suportă play mode.
- `manage_camera screenshot capture_source=scene_view` NU suportă view_position/view_rotation/batch — folosește `batch=orbit` pe game_view (default).
- Conflict cu tasta: PlatformerController.Respawn pe R + GameManager.restart la win pe R = ar declanșa ambele în același frame. Soluție: respawn pe Q, restart pe R.

**REȚETA Meshy „T-pose curat pentru rig" rămâne validă** (vezi mai sus în fișier): `text_to_image` nano-banana-pro cu prompt anti-bias (NU folosi „archer" pt arcaș — folosește „soldier" + listă lungă de NEGATIVE: NO weapons, NO bow, NO quiver) → `image_to_image` pentru BACK view (NU SIDE — regresează la brațe coborâte) → `multi_image_to_3d` cu pose_mode=t-pose, 2 imagini → `rig` cu input_task_id → `animate` cu action_id (lista pe `docs.meshy.ai/en/api/animation-library`). MCP server Meshy ASCUNDE URL-urile image_urls/animation_glb_url — workaround: API key din `~/.claude.json` → REST API direct cu PowerShell.

**STARE FINALĂ SESIUNE 28 mai 2026:**
- Sold Meshy: **946/1024cr** (78 ars pe arcașul player — pipeline validat end-to-end).
- Player Meshy integrat + animat (Idle/Walk/Attack legate de clipuri Meshy în `Assets/Animators/MeshyArcher.controller`).
- Prototip platformer funcțional în Shooter.unity. WASD + SPACE + Shift + Q + GoalZone + 2 țepi.
- ArcherModel Quaternius vechi rămâne în scenă (SetActive=false) ca backup.
- TOATĂ logica veche de luptă (Enemy.cs, EnemyProjectile.cs, Bullet.cs, Burn.cs, BossGate.cs, BossVolley.cs, HeroController.cs, BowVisual.cs) rămâne pe disc — fișierele compilează (stub-uri OnEnemyKilled/OnPlayerDeath în GameManager) dar nu sunt instanțiate prin RoomController/MapBuilder. Le putem șterge curat când suntem siguri că nu le mai vrem.

**PROXIMUL PAS (când reluăm):**
- Userul apasă Play, testează jump/dash/respawn, raportează feel. Acolo iterăm parametri (jumpForce, dashSpeed, dashCooldown).
- Apoi adăugăm complexitate per cameră: platforme suspendate (Cube static, fără Spikes), platforme mișcătoare (component MovePingPong simplu), GoalZone repoziționat sus pe ultima platformă ca să forțezi parcurs.
- Skilluri viitoare (când Spike+Jump+Dash se simt bine): double jump, glide, wall jump, magnet/teleport. Acestea pot reveni ca „upgrade-uri 1-din-3 după cameră" — refolosim partial sistemul ChooseUpgrade scos din GameManager.
- Boss-room (cam 10) = nivel platformer cel mai greu, nu mai e cavaler. Goal violet special.
- HUD: adaug bara de „mid-air remaining" la dash, eventual și jump counter.

---

## ▶️▶️ START AICI MÂINE (final sesiune 27 mai 2026)
**Stare:** joc shooter medieval roguelite COMPLET jucabil & salvat (`Assets/Scenes/Shooter.unity`): hartă 10 camere (inel, uși), arcaș REAL animat (model Quaternius character.glb + Archer.controller cu Idle/Walk/Attack), tir cu arc (draw/release, săgeți arc balistic, sunet real Mixkit), skill-uri foc/pierce/ricoșeu, inamici melee+ranged cu pathfinding (MapNav, nu trec prin pereți), boss (king mare violet, volee) zăvorât cu porți aurii până cureți tot, upgrade 1-din-3, cameră third-person, juice (flash/knockback/poof/sunete). Inamicii + boss-ul sunt ÎNCĂ CAPSULE (doar player-ul are model real).
✅✅ **PILOT MESHY COMPLET 28 mai 2026 — arcaș player integrat în Shooter.unity.** Lanț validat end-to-end. Sold: 1024 → **946** (78cr ars). Vezi [[animal-io-meshy]].

**LECȚIE CHEIE — bias-ul Meshy:** text_to_3d cu cuvântul „archer" → IGNORĂ T-pose + adaugă arc/tolbă/săgeți la model (bias puternic din training). Soluție validată: image-to-3d via nano-banana (genera imagine 2D corect mai întâi, apoi convertită în 3D). Promptul evită „archer" — folosește „soldier" + listă lungă de NEGATIVE (NO weapons, NO bow, NO quiver). În nano-banana-pro: T-pose perfect, fără echipament, exact ce am cerut.

**Pipeline final validat (per personaj player):**
1. `meshy_text_to_image` (nano-banana-pro, ~9cr) → imagine FRONT T-pose, fără arme. Prompt anti-bias.
2. `meshy_image_to_image` cu referință front (~9cr) → BACK view. ⚠️ NU genera SIDE — Meshy interpretează „T-pose în profil" ca brațe coborâte (regresie).
3. `meshy_multi_image_to_3d` (meshy-6, front+back, pose_mode=t-pose, 20cr) → model 3D texturat curat.
4. `meshy_rig` cu input_task_id (5cr, include walk+run gratis cu nume `walking_man`/`running`).
5. `meshy_animate` × 2 (3cr each): action_id=0 (Idle), action_id=224 (Archery_Shot). Lista action_id-uri găsită prin WebFetch pe `docs.meshy.ai/en/api/animation-library`.

**LIMITARE MCP server Meshy:** `text_to_image` și `image_to_image` ASCUND URL-urile rezultate (nu le returnează prin tool); `download_model` nu merge pe taskuri image (eroare „no model URLs available"). **Workaround:** API key e în `C:\Users\Shadow\.claude.json` la cheia `mcpServers.Meshy.env.MESHY_API_KEY` (msy_...). Apel direct la `https://api.meshy.ai/openapi/v1/text-to-image/{id}` / `image-to-image/{id}` / `animations/{id}` / `rigging/{id}` cu `Authorization: Bearer $key` returnează URL-urile (`image_urls[0]`, `animation_glb_url`, `result.rigged_character_glb_url`, `result.basic_animations.walking_glb_url` etc.). PowerShell `Invoke-RestMethod` + `Invoke-WebRequest` descarcă local.

**Integrare în Unity:**
- Toate clipurile Meshy au numele `Armature|<nume>|baselayer` (loop=True implicit). Pattern de import: editor script (`MCP/Build Meshy Archer Controller` + `MCP/List Meshy Archer Clips` în `Assets/Editor/MeshyArcherControllerBuilder.cs`) care extrage primul AnimationClip non-preview din fiecare GLB cu `AssetDatabase.LoadAllAssetsAtPath` și setează `state.motion`. AnimatorController salvat la `Assets/Animators/MeshyArcher.controller` cu 3 stări: Idle / Walk / Attack (numele păstrate identic cu Archer.controller vechi ca să nu modific `HeroController.SetAnim()`).
- ⚠️ glTFast importul GLB-urilor mari (~16MB cu skin+anim) **timeout pe refresh_unity** (>30s). Sleep 60s și verifică `.meta` files manual cu PowerShell — Unity le procesează în background.
- ⚠️ Editorul re-intră singur în play mode des → `manage_scene save/load` eșuează („cannot be used during play mode"). Fix: `manage_editor stop` întâi, apoi save+load.
- **MeshyArcher** instanțiat ca child al Player în Shooter.unity (`localPos [0,-1,0]` — origin GLB la centrul corpului, picioarele ies la sol). ArcherModel Quaternius vechi → SetActive(false), păstrat ca backup. Animator cu `applyRootMotion=false` + `runtimeAnimatorController` = path string către .controller. `BowVisual.cs` procedural rămâne nemodificat (independent de skin, atașat la Player root).

**Refolosire 20cr „ratați" — arcașul v1** (`archer_preview.glb`, pose tragere + arc atașat) → ideal ca INAMIC RANGED (AI static-tragere), nu pierdut.

**Pentru următorii pași** (când reluăm):
- Bilanț credit per personaj real: ~46cr (9 img front + 9 img back + 20 multi-3d + 5 rig + 3 idle + 3 attack/custom). 4 inamici × 46cr = ~184cr, fezabil din 946.
- **Userul testează player-ul în Play mode manual** — fie acum, fie sesiunea viitoare. Așteptare: WASD = clip Walk, stop+țintă = clip Attack (Archery_Shot, 5s loop), stop fără țintă = clip Idle (4s loop).
- Next chars de generat: **inamic melee** (soldat cu sabie), **inamic ranged** (refolosim archer_v1) sau alt arcaș, **boss cavaler** (king violet). Same pipeline.

---

**PLAN (CONFIRMAT de user 28 mai 2026, sesiune nouă):** generăm modelele cu **Meshy-6** rigged+animat. Sold Meshy = **1024 credite**. Pipeline/personaj: text_to_3d Meshy-6 (20cr) → refine textură (10cr) → rig (5cr) → animații custom (3cr each) ≈ **40cr/personaj**. ⚠️ REGULA: confirmă costul cu userul ÎNAINTE de fiecare generare. ✅ **TACTICA PILOT CONFIRMATĂ 28 mai: arcașul ÎNTÂI** (~40cr) ca să validăm lanțul Meshy→Unity înainte să cheltuim pe restul. Generează în T-POSE, FĂRĂ arc în mână (rig curat; arcul rămâne procedural separat). Primul pas: `meshy_text_to_3d` arcaș (20cr) — userul a aprobat strategia pilot care include acest cost. Vezi [[animal-io-meshy]].
**Unity:** dacă e închis, lansează `Start-Process "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" -ArgumentList '-projectPath','C:\Users\Shadow\animal-io'` (~80s boot). ⚠️ Editorul re-intră singur în play mode des → STOP play înainte de set_property/menu-uri editor. Pt animație Meshy în Unity: același pattern ca la player (editor script care alege clipul exact + setează state.motion, fiindcă `controller_add_state` ignoră clip_name; root motion off).

## 🛑🛑 PIVOT MAJOR — 27 mai 2026 (CONFIRMAT DE USER, supersedă TOT ce e mai jos)
**Userul abandonează (temporar) jocul cu lupul/animal-io: „o sa fie foarte complex, hai sa ne axam pe un joc micut simplu, fara complexitate, single player".** Țintă nouă = un joc MIC de tot, single-player, gen **shooting / arcade / candy crush (match-3)**.
**De ce (cinstit, agreat):** ca solo founder nou la game-dev, un joc mic TERMINAT > joc mare neterminat. Învață pipeline-ul complet (input→loop→UI→build→publish), are ce arăta în build-in-public, îl face în zile/săptămâni. Lupul/The Wolf rămâne vis pe raft.
**Cum aplici:** scope mic AGRESIV, un singur gameplay loop, fără feature creep. Tot ce e mai jos (lup realist, pădure Poly Haven, Synty, Meshy animale, Anything World, multiplayer) = CONTEXT/istoric, NU direcția activă. Unity + UnityMCP rămân stack-ul (deja setate). Vezi [[cristian-working-style]] (el dirijează în limbaj natural, eu fac munca brută) + [[user-cristian-dev]].
**Stare:** ✅ GEN ALES = **clonă tip ARCHERO 2** (top-down roguelite shooter). Mecanica-semnătură: eroul trage AUTOMAT în cel mai apropiat inamic DOAR când stă pe loc; te miști = doar te ferești (nu tragi). Cureți camera de inamici → următoarea cameră; la fiecare câteva camere alegi 1 din 3 upgrade-uri random (multishot/săgeți laterale/ricoșeu/atk speed). Mori → restart run. V1 = bucla asta minimă (mișcare+stop-to-shoot, 1-2 inamici, curăță camera→următoarea, 1 set de upgrade-uri, vieți+game over). Construim într-o scenă nouă în proiectul `C:\Users\Shadow\animal-io` (3D/URP, cameră top-down înclinată + forme simple). Lupul/forest izolat, nu-l stricăm. Unity trebuie pornit manual (bridge pe 6400) — la pivot era ÎNCHIS (instance_count 0).

✅✅ MILESTONE 27 mai 2026 — V1 JUCABIL (validat prin screenshot, „funcționează"): scenă `Assets/Scenes/Shooter.unity` cu arenă (podea+4 pereți), cameră top-down înclinată (poz [0,20,-13] rot [57,0,0]), erou (capsulă albă, WASD), inamici roșii care vin spre tine, stop-to-shoot (gloanțe galbene), HUD (Room/Score/HP via OnGUI), camere care se înmulțesc, game over+restart(R). Scripturi în `Assets/Scripts/Shooter/`: `HeroController.cs` (mișcare+auto-fire-când-stai), `Enemy.cs` (chase+melee+registry static), `Bullet.cs`, `GameManager.cs` (spawn camere+scor+HP+HUD), `ShooterUtil.cs` (SetColor URP _BaseColor).
**LECȚII TEHNICE (importante pt sesiunile viitoare):**
- Lansare Unity din PowerShell: `Start-Process "C:\Program Files\Unity\Hub\Editor\6000.4.8f1\Editor\Unity.exe" -ArgumentList '-projectPath','C:\Users\Shadow\animal-io'`. Boot+compile ~75-80s până bridge-ul (port 6402) apare în `mcpforunity://instances`.
- ⚠️ **Proiectul folosește noul Input System** (de la lup) → `UnityEngine.Input` aruncă excepție. Folosește `UnityEngine.InputSystem` (`Keyboard.current.wKey.isPressed`, `.rKey.wasPressedThisFrame`).
- ⚠️ **Coliziuni de nume de clase:** proiectul are sample-uri (`VariableExamples+PlayerController`) → numele „PlayerController" e ambiguu la `manage_components add` prin MCP. Folosește nume unice (am redenumit în `HeroController`).
- ⚠️ **Cameră nouă creată prin MCP = `clearFlags` Skybox fără skybox → ECRAN ALB.** Setează `clearFlags=2` (SolidColor) + `backgroundColor {r,g,b,a=1}`.
- ⚠️ `manage_gameobject create` cu `components_to_add` care include un script custom poate PICA (race cu type lookup) → creează GameObject-ul, apoi adaugă scriptul separat cu `manage_components add`.
- ⚠️ `find_gameobjects` NU vede obiecte create la runtime prin cod (ex. inamici instanțiați) — verifică prin screenshot, nu prin find.
- ⚠️ Bridge-ul MCP e INSTABIL în play mode (erori „Cannot access a disposed object", deconectări, chiar crash Unity o dată). Tactică: intră în play, sleep scurt, UN screenshot, citește consola, apoi STOP play. Nu sta lung în play prin MCP.
- ⚠️ `manage_scene load` cere param `path` (NU `scene_path`).
- ⚠️⚠️ **MCP-driven PLAY MODE crapă Unity** pe setup-ul ăsta — a închis editorul de 2 ori la `manage_editor play` prin MCP (necesită relansare ~80s). Play-ul MANUAL (userul apasă ▶ în editor) e stabil. TACTICĂ NOUĂ: nu mai conduc play mode prin MCP; fac modificările în edit mode (scripturi+set_property+save), apoi userul apasă Play singur și-mi spune ce simte. `manage_components set_property` NU merge în play mode (cere edit mode).

✅ ITERAȚIE DESIGN 27 mai 2026 — DIFERENȚIATOR ales de user: în loc de „shot" (Archero generic), atacul = **AURĂ/ORBITĂ de animal** (refolosește ideea lui de 2 ani [[animal-io-core-design]]). HeroController rescris: când STAI pe loc, o orbită de orbs (3 sfere cyan) se rotește în jur + face damage AoE tuturor inamicilor în rază (auraRadius 3.2, auraDamage 8/tick la 0.22s); te miști = aura se stinge, doar te ferești. Tensiune INVERSĂ față de Archero (acolo ții distanța; aici trebuie să-i lași să se apropie). Userul a zis „prea ușor" → adăugat **inamic RANGED** (mov, `Enemy.Mode.Ranged` în Enemy.cs + `EnemyProjectile.cs`): stă la distanță (6.5) în afara aurei și trage proiectile → te forțează să te miști (pierzi aura) = pedeapsă pt statul pe loc. GameManager: ramp mai dur (count `4+(room-1)*2`, hp `40+(room-1)*12`, speed `3+(room-1)*0.3`), din camera 2 ~1/3 inamici ranged. Reglaje cerute de user (acceptate): proiectile ranged mai lente (speed 9→6.5, interval 1.6→2.0s) + clamp pereți (jucător ±9, inamici ±9.3, fiindcă inamicii se mișcă prin transform și treceau prin pereți). User a zis inamicii sunt „perfecți acum".
✅ FEEDBACK CHEIE user: aura singură = PASIVĂ/plictisitoare PENTRU JUCĂTOR (stai și te uiți). Soluție aleasă: **skill activ POUNCE pe Space** (păstrăm aura ca damage de bază). HeroController: Space → sari spre cel mai apropiat inamic (range 5.5, durată 0.16s) cu burst AoE la aterizare (pounceDamage 30, hitRadius 2.0), cooldown 1.3s; bară de cooldown în HUD (`PounceCharge` 0..1 + label „POUNCE ready (Space)"). Acum jucătorul are skill de exprimat. URMĂTOR posibil: cârligul upgrade 1-din-3; mai multe animale/skill-uri; juice. User testează manual.
**LECȚIE DESIGN:** când propui o mecanică, întreabă-te dacă e activă PT JUCĂTOR (input/decizii), nu doar „corectă" matematic — aura pasivă era ok ca sistem dar plictisitoare de jucat.

🔄 CORECȚIE 27 mai (user): aura+pounce = scoase din base; **base = doar SHOT (auto-tragere când stai)**, iar skill-urile „să vină pe parcurs" → am construit **sistem de progresie 1-din-3 upgrade după fiecare cameră** (GameManager: `choosing` + Time.timeScale=0 + alegi cu taste 1/2/3 prin OnGUI; HeroController are hooks `UpgAttackSpeed/UpgDamage/UpgProjectile(multishot spread)/UpgMoveSpeed/UpgBulletSpeed/UpgMaxHP/UpgArmor`). Pounce/aura pot reveni ca upgrade-uri deblocabile.

🎨 TEMĂ ALEASĂ (user, 27 mai): **MEDIEVAL.** Ești arcaș, proiectilele = SĂGEȚI (nu gloanțe/pistol) — schimbat vizual: săgeata = cub alungit (0.1×0.1×0.85) culoare lemn (0.85,0.7,0.45), orientat pe direcția de zbor via Bullet.Launch LookRotation. Skill-urile/echipamentul = ARMURĂ + SCUT + arme medievale, cu „modele diferite, efecte și sunete". Upgrade-urile re-tematizate medieval (Arc rapid, Varf ascutit, Sageata dubla, Cizme usoare, Sageti grele, Armura, Scut=-15% damage primit via damageReduction 0..0.7). DE FĂCUT (assets, etapă viitoare): modele 3D reale armură/scut/arc (Synty deja în proiect SAU Meshy [[animal-io-meshy]]) + efecte + sunete hit/trage.

✅ PASS FEEL+SUNET (27 mai): adăugat juice — `Sfx.cs` (SUNET PROCEDURAL generat în cod la runtime, ZERO fișiere audio: Pluck=tonuri glisante pt tras/durere, Noise=burst filtrat pt lovit/moarte; redat 2D prin AudioSource ascuns, AudioListener e pe Main Camera). `Poof.cs` (PoofBit: burst de sfere care zboară+se micșorează la moartea inamicului; static `PoofBit.Spawn(pos,color)`). Enemy: flash alb la lovire (lerp înapoi în Update), knockback 0.22 dinspre player, sunet hit/death, poof la moarte, flag `dying`. HeroController: `Sfx.Shoot()` la volley, `Sfx.Hurt()` la damage. TEHNICĂ DE REȚINUT: sunet 100% procedural (AudioClip.Create+SetData) = fără assets, perfect pt prototip. URMĂTOR posibil: numere de damage flotante (TextMesh — atenție la font builtin), camera shake, apoi modele medievale reale (Synty/Meshy) + boss.

✅ TIR MEDIEVAL „nu tip joc" (27 mai, user a cerut „full"): (1) CADENȚĂ draw→release în HeroController — când stai pe loc + ai țintă, `drawing`/`drawT` acumulează până la fireInterval (base urcat 0.45→0.7 = greutate), se aude coarda la start de draw, apoi ReleaseArrow (thwip + spawn săgeți multishot); mișcarea anulează draw-ul. fireInterval = timp de întindere, scurtat de UpgAttackSpeed. (2) SUNETE de arc în Sfx: `Creak` (zgomot filtrat rising+tremor = coardă întinsă) + `Thwip` (snap lemnos + whoosh descendent = eliberare), înlocuiesc twang-ul. (3) Bullet rescris cu TRAIECTORIE BALISTICĂ: vy0=3.6 + gravity -16, se înclină pe velocity (Orient), hit-test XZ pe rază (ignoră Y), iar la ratare se înfige în sol (resting la y=0.18, linger 0.6s). bulletSpeed base 16.
✅ ITERAȚIE „aproape real" (27 mai): user a cerut arc VIZIBIL + sunet mai real. (a) `BowVisual.cs` pe Player — arc procedural din 2 LineRenderer (limbe lemn curbate cu sin + coardă în 3 puncte care se trage înapoi după `hero.DrawNormalized`) + săgeată pe coardă (cub) activă doar la draw; HeroController expune `IsDrawing`/`DrawNormalized`. Material LineRenderer = Shader „Sprites/Default". DE VERIFICAT cu user: vizibilitate/orientare din camera top-down înclinată. (b) Sfx Creak rescris cu sample-and-hold stepped (textură de scârțâit) + Thwip cu attack/body/whoosh layered. ⚠️ LIMITĂ: sunetul rămâne SINTETIC; pt 100% real = sample-uri .wav CC0 (i-am oferit userului să caut un pachet gratis dacă synth-ul nu convinge).

🗺️ RESTRUCTURARE MARE 27 mai — „joc de răbdare și logică": user a cerut hartă de 10 CAMERE legate (a ales GRILĂ/hărțișoară, NU coridor/ramificat), uși DESCHISE (nu se blochează), fiecare cameră se activează când intri (spawn inamici), o cureți → upgrade → mergi mai departe; toate 10 = VICTORIE. NU mai e arenă cu valuri. Implementat (verificat prin screenshot că harta se construiește corect = inel 4×3 cu mijloc gol + uși):
- `MapBuilder.cs` (pe obiect „Map"): generează în cod 10 camere (inel pe grilă 4×3, `cells` static), podele+pereți cu GOLURI de UȘĂ spre vecini, RoomController per cameră; mută Player în camera start; `Start()` setează GameManager.SetTotalRooms (mut din Awake ca să existe Instance).
- `RoomController.cs`: la player-in-bounds (verificat în Update, nu trigger) → Activate → spawn inamici (count=3+tier, scalează cu tier-ul camerei, mix melee/ranged); `OnEnemyKilled` decrement → la 0 `GameManager.OnRoomCleared`.
- `ShooterCamera.cs` (pe Main Camera): URMĂREȘTE jucătorul (offset [0,18,-11], rot 57, lerp). ⚠️ Numele `CameraFollow` era LUAT de prototipul vechi → coliziune CS0101, am redenumit în ShooterCamera.
- GameManager rescris ROOM-BASED: Score, RoomsCleared/TotalRooms, OnRoomCleared (upgrade 1-din-3 + win la total), HUD „Camere X/Y", ecran VICTORIE + Game Over, restart R.
- Enemy: +`SetRoom(room,center,half)` → notifică camera la moarte + clamp în limitele CAMEREI (nu ±9.3 global). HeroController: scos clamp-ul ±9 (pereții îl țin fizic acum).
Scenă Shooter.unity salvată cu Map+MapBuilder+ShooterCamera; arena veche (Floor+4 walls) ștearsă. User testează manual (play prin MCP tot crapă/dă alb intermitent — dar captura scene_view + positioned [view_position]/[view_rotation] randează ok, folosește-le pt verificare vizuală).

✅ REGLAJE 27 mai (post-test user „super fain"): (1) BOOST la tragere — userul simțea wind-up-ul prea lung la fiecare oprire; HeroController: flag `justStopped` (set la mișcare) → la oprire prima săgeată zboară INSTANT (ReleaseArrow), apoi cadență normală; fireInterval base 0.7→0.55. (2) SUNET REAL de arc: descărcat MP3 royalty-free de pe BigSoundBank (s0549 „Archery", comercial-ok fără atribuire) în `Assets/Resources/bow_release.mp3` (WAV-ul lor dădea 404, MP3 ok ~39KB); Sfx.Ensure încarcă `Resources.Load<AudioClip>("bow_release")` cu fallback la Thwip synth. ⚠️ SURSE AUDIO LICENȚĂ: OpenGameArt bow era CC-BY-SA (share-alike viral, evitat pt comercial); BigSoundBank/Pixabay = royalty-free comercial OK. Draw rămâne synth (Creak). Pt mai multe sunete reale: același pattern (download în Resources + Resources.Load).

✅ FIX-uri 27 mai (2): (1) PROIECTILE prin pereți — userul a reclamat că shoturile boților (și săgețile) treceau prin pereți. Fix: OnTriggerEnter în `EnemyProjectile` + `Bullet` distruge proiectilul când lovește orice collider care NU e Enemy/HeroController/alt proiectil (= perete/obstacol). Trec doar prin uși (golurile n-au collider). (2) SUNET arc înlocuit — cel de pe BigSoundBank (s0549) era „x2" (dublu) = suna prost; descărcat în loc **Mixkit „arrow-whoosh-1491"** (https://assets.mixkit.co/active_storage/sfx/1491/1491.wav, licență Mixkit free comercial fără atribuire) în `Assets/Resources/bow_release.wav` (șters mp3-ul vechi). E mai mult „whoosh de săgeată" decât „twang de coardă" — de verificat cu userul dacă vrea pocnetul corzii.
✅ Și: inamicii NU mai sunt limitați la camera lor (scos clamp-ul) — te urmăresc oriunde după activare (cerut de user: „ai intrat nepregătit"). ⚠️ Ei se mișcă prin transform (fără pathfinding) → trec prin pereți când te urmăresc în alte camere; fix corect = NavMesh (oferit userului, neînceput).

✅ SKILL-URI DE SĂGEATĂ 27 mai (user a ales „skill-uri de săgeată" din 4 opțiuni; eu i-am explicat că sabie/iatagan = melee = sistem separat, lăsat pe mai târziu): adăugat 3 skill-uri build-defining ca upgrade-uri (pool acum 10, oferă 3 random/cameră): 🔥 Săgeți de foc (`UpgFire` → burn DoT prin `Burn.cs` component pe enemy, 8dps 2.5s, săgeata devine portocalie), 🏹 Perforante (`UpgPierce` → trece prin +N inamici), ↩️ Ricoșeu (`UpgBounce` → sare la următorul). Bullet rescris cu pierce/bounce/burn + `Configure()` + listă `hits` (nu re-lovește același). Enemy.TakeDamage acum are overload `(dmg, bool fx)` ca arderea să nu spameze flash/sunet. Se combină între ele + multishot. URMĂTOR posibil: mai multe skill-uri/raritate, echipament cu modele (scut/armură), boss, sau melee (sabie/iatagan).

✅ PATHFINDING + LINIE-DE-VEDERE 27 mai (bug user: bot blocat în perete + tu tragi prin zid): (1) `MapNav.cs` static — graf de camere (centre + adiacență ortogonală din `cells`, populat de MapBuilder via `MapNav.Init`), `NextWaypoint(from,to)` = BFS prin camere, întoarce midpoint-ul ușii spre camera următoare; Enemy se mișcă spre waypoint (ocolește pereții prin uși) în loc de linie dreaptă spre player. (2) `ShooterUtil.WallBetween(a,b)` = raycast care detectează perete între 2 puncte (ignoră Enemy/Hero). Folosit de: HeroController `NearestVisible()` (țintește doar inamici cu linie liberă — nu mai trage prin zid), și Enemy ranged (trage doar când are LoS clar). ⚠️ Navigația e pe grilă (țintesc centrul ușii), enemies tot se mișcă prin transform (nu fizică) → pot „tăia colțul" pe la marginea ușii; dacă deranjează, refine (NavMesh real). User testează.
🐛 FIX follow-up: prima versiune a pathfinding-ului ținta exact mijlocul ușii (pe linia peretelui) → boții se opreau pe prag și NU treceau în camera următoare. Fix: `NextWaypoint` întoarce acum door+axis*3 (un punct dincolo de ușă, în camera următoare) ca să TREACĂ prin ea, apoi recalculează.

✅ BOSS 27 mai (user a ales „boss în ultima cameră"): ultima cameră (idx == cells.Length-1) = boss room. Enemy extins cu `isBoss`/`MakeBoss()` (scale 2.2, touchDamage 26, HP ~320+tier*18, culoare violet) + `BossVolley` (8 proiectile în evantai la `bossFireInterval` 2.2s, doar cu LoS). `Enemy.ShootAtPlayer`→redenumit `FireProjectile` (refolosit de ranged + boss volley). `Enemy.CurrentBoss` static + `HPFraction` → GameManager OnGUI desenează BARĂ DE BOSS sus când CurrentBoss!=null (Unity fake-null gestionează restart-ul). RoomController.Configure are acum param `bossRoom`; Activate spawnează 1 boss (alive=1) via SpawnBoss. Boss dobor→cameră curățată→win dacă toate 10. URMĂTOR sugerat: modele 3D reale sau meniu start.
🐛 FIX design (user): pe inel ajungeai la boss înainte să cureți tot → boss nu era finalul. Fix: `BossGate.cs` (porți aurii pe ușile camerei boss, marker + listă statică `All` + `OpenAll`). MapBuilder creează gate-uri (cuburi cu collider, doorWidth) pe ușile camerei boss; GameManager.OnRoomCleared cheamă `BossGate.OpenAll()` când RoomsCleared >= TotalRooms-1 (toate non-boss curățate). Boss = garantat ultimul, indiferent de drum.
🐛 BUG IMPORTANT rezolvat (user: „armele nu se activează cum trebuie / țin un timp scurt"): `ShooterUtil.WallBetween` (raycast LoS) lovea propriile SĂGEȚI/proiectile în zbor (trigger collidere între player și inamic) → le confunda cu pereți → arcașul se oprea din tras intermitent (idem boții ranged). FIX: raycast cu `QueryTriggerInteraction.Ignore` (+ `Physics.DefaultRaycastLayers`) → ignoră trigger-ele, lovește doar pereții solizi. Skill-urile erau OK (permanente) — doar tragerea de bază se întrerupea. LECȚIE: Physics.Raycast lovește trigger-e by default (queriesHitTriggers=true).

✅ MODELE 3D — FAZA 1 (player), 27 mai: user a ales „pachet medieval gratis". Synty Starter din proiect = personaje GENERICE moderne (cop/cowboy/male), NU medievale → nefolosibil. Sursă bună = Quaternius CC0 via poly.pizza CDN (ca animalele): extras URL GLB real din HTML-ul paginii /m/ cu regex pe `static.poly.pizza/<uuid>.glb`. Descărcat în `Assets/Models/`: `character.glb` (Quaternius „Character Animated", hero/RPG, ANIMAT — clipuri Idle/Walk confirmate) + `king.glb` (Quaternius „King", medieval, pt boss). Importate cu glTFast (⚠️ importul GLB mare a închis temporar bridge-ul ~20s, a revenit). PLAYER: instanțiat `character.glb` ca child „ArcherModel" sub Player (localPos y=-1 → picioare la sol, scale 1), capsula ascunsă (`MeshRenderer.enabled=false`), Animator cu `Assets/Animators/Archer.controller` (Idle default + Walk, FĂRĂ tranziții — condițiile prin MCP n-au mers); HeroController.SetAnim() face `anim.CrossFade("Walk"/"Idle",0.12)` la schimbarea mișcării. VERIFICAT prin screenshot scene_view: arată ca un om real low-poly. ⚠️ Tranzițiile Animator prin `controller_add_transition` n-au primit condiția (0 conditions) → condus din cod cu CrossFade. ⚠️ `controller_add_parameter type:Bool` a ieșit float.
RĂMÂNE FAZA 2: modele pe INAMICI + BOSS. Problemă: avem 1 singur model uman → inamicii ar arăta ca player-ul; soluție = tentă (roșu/mov) ca să se distingă, boss = king.glb (mai mare). Pt instanțiere la runtime din cod: GLB-urile trebuie în `Assets/Resources/` (Resources.Load) SAU referință serializată. Neînceput.
✅ Cameră third-person (user a cerut „în spatele lui, mai jos, dar să vadă tot"): ShooterCamera offset [0,11,-12] tilt 42° (era [0,18,-11]/57°), orientare FIXĂ (nu se rotește la re-țintire). Mișcare mai naturală: rotație lină `FaceDir` (turnSpeed 540°/s, RotateTowards) în loc de snap + accelerare/decelerare (`accel` 28, Vector3.MoveTowards pe `vel`). Personajul se întoarce spre direcția de MERS când merge, spre țintă când stă.
⚠️ ANIMAȚIE GLB Quaternius: (1) clipurile NU sunt marcate looping la import glTFast → înghețau după un ciclu (părea „zboară"); fix = loop-guard în Update (`if normalizedTime>=1 && !IsInTransition → anim.Play(fullPathHash,0,nt%1)`). (2) Root motion ON mișca corpul peste mișcarea noastră → `anim.applyRootMotion=false` în Awake. LECȚIE pt FAZA 2 (inamici/boss animați): aplică root motion off.
🐛🐛 BUG REAL al mersului (rezolvat): `manage_animation controller_add_state` IGNORĂ `clip_name` și leagă MEREU primul clip (alfabetic „Attacking_Idle") → ambele stări Idle+Walk jucau aceeași poză → personajul „aluneca/zbura". Clipurile reale din character.glb (verificate cu script editor `ClipLister`): `CharacterArmature|Idle` (3.1s), `CharacterArmature|Walk` (1.25s), `CharacterArmature|Run` (0.875s), `CharacterArmature|Dagger_Attack`, etc. — TOATE loop=True (deci loop-guard inutil, scos). SOLUȚIE: NU folosi controller_add_state pt clip specific; construiește AnimatorController cu SCRIPT DE EDITOR (`Assets/Editor/ArcherControllerBuilder.cs`, menu „MCP/Build Archer Controller") care alege clipul exact prin C# (`AssetDatabase.LoadAllAssetsAtPath` + match pe `c.name`), setează `state.motion`, și asignează `Animator.runtimeAnimatorController` pe ArcherModel în scenă. ✅ Confirmat în play: clipuri corecte Idle/Walk bound. Pt inamici/boss animați = același pattern (editor script per clip). Glb importat cu glTFast: clipuri cu prefix `CharacterArmature|` (+ duplicate fără prefix + fără armatura).
✅ STARE ATTACK (user: „arcul trage singur, mâinile rămân pe lângă corp" când trage): adăugat în controller starea „Attack" = `CharacterArmature|Attacking_Idle` (stance de luptă, brațe sus). HeroController.SetAnim acum: Walk (mișcare) / Attack (stă + are țintă vizibilă, trage) / Idle (stă degeaba). ⚠️ Pachetul „Character Animated" e ROGUE cu pumnale, n-are animație de ARC — Attack e stance generică (sincronizată cu arcul procedural). Variante dacă userul vrea mai fidel: alt model cu anim de arc, sau „Dagger_Attack" one-shot per săgeată. ⚠️ Editorul RE-INTRĂ singur în play mode des → menu-urile editor (BuildArcherController) eșuează în play („cannot be used during play mode"); STOP play înainte de a rula menu-uri editor.

🌐 IDEE user (de ținut pt mai târziu): vrea eventual MULTIPLAYER online PvP (jucători reali unii contra altora, nu boți). I-am explicat: real-time PvP = complexitate mare (netcode/server) = capcana de care a fugit la lup. ALTERNATIVĂ ieftină recomandată = PvP ASINCRON/ghost + leaderboard global (backend clasic, terenul lui web/backend). Plan: termină single-player întâi, multiplayer asincron = faza 2.

---

**DECIS (25 mai 2026): `animal-io` se face WEB/JS, NU Unity.** Motiv: în Unity „nu înțelege nimic", web e limbajul lui (dev web/backend) + iterație instantă + **țintă Poki**. Unity rămâne instalat pt eventual mobil/Steam mai târziu (nu se pierde nimic). Diverge de planul master (Unity/Android) — varianta web e acum sursa activă.

⚠️⚠️ NU SUGERA UNITY CA MOTOR PT ACEST JOC. Motivul pivotului = **Poki** (portal web, jocuri ușoare care pornesc instant). **Unity exportat WebGL e greoi/lent → prost pt Poki.** Am derapat o dată sugerând Unity pt arta Synty; userul m-a corectat („de aia am pivotat"). Corecția corectă: **arta Synty (modelele low-poly) se folosește pe WEB convertind FBX→GLB** și încărcând în Babylon — NU prin motorul Unity. Modelele Synty sunt ușoare, merg pe web, păstrează Poki. Singurul cost = conversia FBX→GLB (o fac eu; static/props sigur, personaje animate pot cere ajustări). Look-ul = „Clash-inspirat premium", nu 100% Clash (ăla = bespoke Supercell, irealizabil solo+ieftin); și pt Poki clean-premium e oricum mai potrivit decât Clash.

**Sursa de adevăr = `C:\Users\Shadow\animal-io\animal-io-PROIECT-MASTER.md`** (+ `plan-joc-io.md`, `setup-joc-unity.md`). Citește-l ÎNTÂI; e detaliat și matur. Planul oficial: Unity 6 URP, **2.5D izometric stil Clash of Clans**, Android-first, backend Railway+Supabase (faza 3+), monetizare Ads/IAP, faze single-player+boți → multiplayer echipe 4v4/3v3 → brackets.

**Deja FĂCUT (de verificat în repo, nu presupune):**
- Prototip web jucabil: `animal-io\prototype\animal-io.html` — clonă agar.io completă vanilla JS+Canvas (mouse move, split/eject, 25 boți AI, viruși, clasament, minimap, creștere, moarte/respawn). Bucla de bază merge.
- **Pipeline assets AI (secț. 9b)** = „soluțiile externe care le creează ei pentru noi": **Meshy** (+MCP) generează animale 3D rigged+animate, export GLB/FBX; **Blender MCP** cleanup/medii; **Tripo** props. Cristian NU e artist 3D — AI-ul + Claude fac arta. GLB se încarcă nativ și în engine web 3D (Three.js/Babylon/PlayCanvas), deci pipeline-ul merge și pe web.

**Folder varianta web: `C:\Users\Shadow\animal-io-web`** — conține `DESIGN.md` (design complet, decizii bătute în cuie), `ROADMAP.md` (7 faze), și build-ul live: `index.html` + `js/game.js`. **Stare (25 mai 2026): Faza 1 — 2.5D jucabil.** Motor = **Babylon.js** (3D web, de pe CDN, fără build/npm; deschis cu `Start-Process index.html`, file://). Conține: hartă cu grilă + pereți + lumină/umbre, șoricel placeholder (sferă+nas) mișcat după mouse (picking pe sol), cameră înclinată 2.5D care urmărește, **Dash pe SPACE** (cooldown, ca DATE `player.skill`), **3 boți** (sfere roșii) cu AI care rătăcesc + fug de jucător, **prindere → scor „Prinși" + creștere vizibilă** (scalare mesh, placeholder pt evoluție). Fix-uri făcute: stutter (eliminat alocări Vector3/frame) + vibrație cursor-pe-jucător. **+ Sistem de LEVEL-URI funcțional (`startLevel`/`completeLevel`):** condiție de victorie (prinde `goal = 5+n*3`), overlay „Level X complet!", apoi level n+1 mai greu (boți mai mulți+rapizi: count `2+n`, speed `9+n*2`), jucătorul „o ia de la zero" (mărime resetată) la fiecare level. HUD: „Level X · Prinși: c / goal". Bucla completă de joc merge. Userul confirmat „funcționează". Următori pași posibili (NU începute): grafică reală (Meshy GLB — cere cont Meshy de la user), mai multe animale/skilluri/tier-uri, polish, sunet.

**UPDATE (25 mai 2026): primul asset real integrat.** Player-ul (era sferă-placeholder) a fost înlocuit cu **șoricelul GLB Meshy riggat** (vezi [[animal-io-meshy]]): `animal-io-web\assets\mouse_walk.glb` (mesh+schelet+walk+textură). În `game.js`: încărcare cu `BABYLON.SceneLoader.ImportMesh`, auto-fit pe bounding box (`MOUSE_HEIGHT`, tălpi la y=0 via pivot intermediar ca să nu stricăm `__root__`), animația de mers redată/oprită după mișcare, `MOUSE_YAW` pt direcție. Camera coborâtă (`CAM_H=15`, `CAM_BACK=17`, `CAM_LOOK_Y=1.4`, lerp 0.16) — userul o simțea „prea de sus / nu e conectat". Adăugat **`babylonjs.loaders.min.js`** în index.html + **`server.js`** (Node static pe :8080) — OBLIGATORIU: GLB-ul NU se încarcă pe file:// (CORS), trebuie servit HTTP. Rulare: `node server.js` din `animal-io-web`, apoi http://localhost:8080. Boții au rămas sfere roșii (încă neînlocuiți). Caveat știut: fața se deformează la mers (auto-rig) — lăsat așa deocamdată.

**UPDATE 2 (25 mai 2026): decor real + strategie assets clarificată.** Userul a fost frustrat că placeholder-ele (forme geometrice) arătau groaznic și a întrebat cum face harta fără 100 de generări. DECIS: **strategie hibridă** — mediu (case/copaci/pietre) din **pachete CC0 gratis gata făcute**, assets UNICE (animale + cașcaval=mâncarea de creștere) din Meshy premium. Niciun pachet gratis nu dă fix Brawl Stars (ăla = Synty plătit ~$30-100); CC0 (Kenney/Quaternius) = low-poly curat, mai plat dar frumos+coerent. Userul a ales pachet CC0 gratis pt mediu.
Implementat: descărcat **Kenney Nature Kit** (CC0) la `animal-io-web\assets\kits\nature` (329 GLB), copiat 13 curate în `animal-io-web\assets\env` (tree1-4, rock1-3, bush1-2, flower1-2, grass1, mushroom1). În game.js: `placeEnv(file,x,z,targetH)` încarcă GLB-ul, auto-scale pe bbox, bază la sol, anchor(poz+rot)+pivot(scară), contur cartoon. `scatter()` împrăștie ~67 obiecte lăsând centrul liber. Toate cu `outline()` (renderOutline negru, semnătura Brawl/Clash) — aplicat și pe șoricel+boți. Boții = acum modelul de șoricel tentat roșcat (`applyTint`), nu mai sunt sfere.

✅ PIPELINE SYNTY→WEB VALIDAT (25 mai 2026): userul a luat **Starter Pack-ul GRATIS Synty** (în Downloads: SourceFiles zip = 70 FBX + 4 atlase PolygonStarter_Texture_0X.png; mai sunt variante Unity/Unreal/Godot). Convertor **FBX2glTF v0.9.7** (binar Windows, fără cont) descărcat la `animal-io-web\tools\FBX2glTF.exe`. Comandă: `FBX2glTF -i x.fbx -o out --binary` (NU --embed). ⚠️ Textura NU se încorporează (FBX referă atlasul prin ID intern „file284") — soluția: aplici manual atlasul în Babylon pe materialul fiecărui mesh (`mat.albedoTexture = SYNTY_ATLAS`), UV-urile sunt deja în GLB. Atlas încărcat cu invertY=false + NEAREST sampling (palette). 10 modele convertite în `animal-io-web\assets\synty\` (s_tree1-4, s_rock1-3, s_stump, s_house, s_crate) + texturi. În game.js: `placeSynty()` + `scatter(..., placeSynty)`; harta acum populată cu Synty în loc de Kenney. Sursa extrasă în `assets\synty_tmp` (de curățat). DE VERIFICAT cu userul: culori/orientare atlas, casa (alt sub-pachet). Pachetul gratis n-are ANIMALE (alea = alt pachet Synty plătit sau Meshy).

⚠️ ÎNCĂ DESCHIS: userul a zis că ȘORICELUL tot nu arată cum vrea. Plan agreat dar neînceput: concept 2D (Meshy text-to-image nano-banana, 3-9 cr) în stil Brawl Stars → reglat pe imagine → image-to-3D. De făcut next. Casele lipsesc (Nature Kit n-are; City Kit Kenney la nevoie).

## ✅✅ DECIZIE STRATEGICĂ FINALĂ (25 mai 2026) — DUBLĂ PISTĂ
Userul a rezolvat tensiunea web-vs-Unity: **facem AMBELE, pe platforma potrivită fiecăreia.**
1. **WEB / Poki = 2D.** Versiune 2D top-down (pe baza clonei agar.io `animal-io\prototype\animal-io.html` care „a ieșit senzație"). Ușor, instant-play, Poki-friendly, exact ce iese bine în Canvas/JS. Pentru reach/marketing.
2. **UNITY = „jocul adevărat".** Jocul 3D premium real: Synty nativ (import 1 click, arată ca în reclame) + URP + post-procesare = look-ul Clash. Țintă mobil/Steam (= planul MASTER original). Claude conduce Unity prin Unity MCP; userul NU trebuie să înțeleagă Unity.

⚠️ CONSECINȚE:
- **Experimentul Babylon 3D-pe-web (`animal-io-web`) e ABANDONAT** ca direcție de produs (rămâne pe disc ca referință). Web-ul NU mai e 3D Babylon — e 2D. Nu mai investi în animal-io-web ca joc.
- De ce 3D-pe-web a fost „varză": amestec de assets nepotrivite (AI+Kenney+forme) + pe web reconstruiești arta premium (reaplici texturi manual, fără materialele/prefab-urile/luminile studioului) → pierde mereu un nivel. În Unity moștenești totul → arată ca demo-ul.
- Synty pe Unity = nativ (vezi [[animal-io-meshy]] pt pipeline-ul de assets; Meshy rămâne util pt animale unice). FBX2glTF + atlas manual (folosit pt testul web) NU mai e necesar pe pista Unity.

Vezi [[animal-io-setup]] pt mediul deja instalat (Unity 6 + URP + Unity MCP + Node).

## ▶️ START SESIUNE URMĂTOARE (decis 25 mai 2026, final de sesiune)
- **Focus Claude+user = UNITY (jocul adevărat 3D).** Userul se ocupă SINGUR de versiunea 2D web/Poki — NU o mai facem noi. Nu redeschide discuția web-vs-Unity, e bătută.
- **Confirmat (sesiunea asta) că Poki aduce bani:** model 100% reclame, rev-share 50/50 (trafic Poki) / 100% (trafic adus de user), fără integrare de plată, dar SELECTIV (aplici, te evaluează). Asta justifică pista 2D — dar userul o face singur.
- **Primul pas Unity:** import Synty + șoricel care se mișcă pe hartă 2.5D izometrică cu look premium (URP + post-procesare). Claude conduce Unity prin Unity MCP (userul nu trebuie să-l înțeleagă). ⚠️ Tool-urile UnityMCP se încarcă doar la pornirea Claude Code; verifică `claude mcp list` / că Unity Editor e deschis cu bridge-ul activ.
- **Synty Starter Pack GRATIS deja descărcat** în `C:\Users\Shadow\Downloads\`: `POLYGON_Starter_Unity_2022_3_v1_2_0.unitypackage` (108MB — ăsta se importă NATIV în Unity, 1 click, cu prefab-uri/materiale/scenă demo) + variante Unreal/Godot/SourceFiles. Pe pista Unity folosim .unitypackage direct (NU conversia FBX→GLB).
- Pachetul Starter **n-are animale** — pt șoricel/pisică/lup/leu: alt pachet Synty (abonament ~$30/lună = toată biblioteca, descarci, anulezi) SAU Meshy ([[animal-io-meshy]]).
- Assets web abandonate rămân pe disc ca referință (`animal-io-web\`, `assets\synty`, `assets\env`, `assets\kits`) — nu s-au șters.

## ✅ PROGRES SESIUNE 26 mai 2026 (Unity)
- **Conexiune Unity↔Claude REPARATĂ DEFINITIV** (era blocată de un mismatch de transport stdio/HTTP). Detalii + fix permanent în [[animal-io-setup]]. De-acum: deschizi Unity → bridge auto pe 6400 → Claude îl vede. Verifică `mcpforunity://instances` (instance_count>=1).
- **Synty Starter IMPORTAT NATIV** în `C:\Users\Shadow\animal-io\Assets\Synty` (importat prin MCP cu un script Editor `Assets/Editor/SyntyImporter.cs` + menu item `MCP/Import Synty Starter`, fiindcă `execute_code` pică pe Unity 6 — linia de comandă mono depășește limita Windows; folosește calea cu script Editor + `execute_menu_item` pentru cod C# de rulat). Conține 546 prefab-uri (Characters umanoide, Environment, Building, Props, Weapons), 117 materiale, 124 texturi, 3 scene demo. Synty = MEDIUL premium; animalele vin din Meshy.
- **EXISTĂ DEJA un prototip Unity** din 24 mai (dinainte de tura web), în scena `Assets/Scenes/Game.unity`: `Player` (sferă cu `PlayerMovement`+`FoodCollector`+Rigidbody), 4× `Food` (tag Food), `Ground` (plane+MeshCollider), `Main Camera` cu `CameraFollow`, Directional Light. Scripturi în `Assets/Scripts/`: `PlayerMovement.cs`, `CameraFollow.cs`, `FoodCollector.cs`. URP mobil deja configurat (Settings/Mobile_*). Deci scheletul agar pe placeholdere există; lipsește arta premium + animalul real.
- **✅ ROSTER DE ANIMALE ANIMATE GRATIS (CC0) — integrat în Unity:** 12 animale Quaternius „Ultimate Animated Animal Pack" (Fox, Wolf, Husky, ShibaInu, Bull, Cow, Donkey, Horse, WhiteHorse, Deer, Stag, Alpaca), **fiecare cu ~24 animații** (Walk, Gallop, Idle, Jump, Attack, Death, Eating, HitReact...). În `Assets/Animals/*.glb`. Sursă download direct (fără cont): CDN poly.pizza `https://static.poly.pizza/<uuid>.glb` (UUID-urile per animal le-am extras din paginile /m/ de pe poly.pizza). CC0 = comercial ok. Astea acoperă scara tier-urilor din [[animal-io-core-design]] (Fox/animale mici → Bull/Horse mari). NU sunt șoricel/leu/tigru/elefant — alea din Meshy ([[animal-io-meshy]]) sau alt pachet la nevoie.
- **⚠️ GLB în Unity 6 = glTFast, dar versiunea CONTEAZĂ:** Unity nu importă GLB nativ. Instalat `com.unity.cloud.gltfast` în `Packages/manifest.json`. **6.10.3 are un BUG (Issue #772): importul în editor al mesh-urilor skinned eșuează cu `SortAndNormalizeBoneWeightsJob ... JobHandle.Complete()`.** Fix = urcat la **6.15.1** (a tras automat și `com.unity.shadergraph`). După upgrade, reimport (`manage_asset action=import` pe fiecare .glb, sau batch) → se importă ca `UnityEngine.GameObject` cu Animator + clipuri. Pentru viitor: dacă mai pică importuri GLB, verifică versiunea glTFast.
- **⚠️ Asignare AnimatorController prin MCP:** `runtimeAnimatorController` se setează cu **calea ca STRING** (`"Assets/.../X.controller"`), NU ca obiect `{path:...}` (ăla dă eroare). Controller creat cu `manage_animation controller_create` + `controller_add_state` (properties: `state_name`, `clip_path`=glb, `clip_name`, `is_default`).
- **✅ DEMO VALIDAT (26 mai):** Fox_Demo pus în `Game.unity` cu `FoxDemo.controller` (default state Walk) → Play mode → vulpoi low-poly animat mergând cu umbre pe teren. Screenshot în `Assets/Screenshots/`. Userul a văzut mișcarea reală. CONFIRMĂ pipeline-ul: animale gratis animate → Unity → se mișcă realist.
- **NEÎNCEPUT — următorul pas natural:** integrarea reală în gameplay — blend tree (Idle↔Walk↔Run pe viteză) pe player, înlocuirea sferei Player cu un animal, + mediul premium Synty + cameră 2.5D izometrică. (Fork-ul vechi a/b/c e depășit — avem deja animale animate în scenă.)

## 🔄🔄 PIVOT MAJOR DE VIZIUNE — 26 mai 2026 (CONFIRMAT DE USER, supersedă TOT ce e mai sus)
⚠️ **Userul a clarificat în sfârșit referința exactă: jocul = ca THE WOLF (Swift Apps, „The Wolf: Animal Game MMORPG", Google Play/AppStore).** Confirmat explicit: „exact așa" — atât GRAFICA cât și ANIMALUL. **Asta REDEFINEȘTE proiectul. Toate deciziile cartoon/2.5D/Brawl/agar/Poki de mai sus sunt ABANDONATE.**

**Ce e The Wolf (noua țintă):**
- **3D REALIST la persoana a treia**, cameră ÎN SPATELE animalului (NU top-down, NU 2.5D izometric, NU .io plat).
- **Grafică realistă** — pădure densă, munți, râuri, lumină/umbre/vegetație bogată, animale care arată „reale" (NU low-poly cartoon).
- Ești un **LUP realist** care explorează lume 3D imersivă, vânează (lanț trofic: șobolan/iepure → vulpe/raton → bizon/taur), RPG (skill-uri, atribute, devii Alpha), MMO real-time (co-op + PvP).
- Lanțul trofic din [[animal-io-core-design]] SE PĂSTREAZĂ ca idee, dar în 3D realist, nu cartoon.

**CONSECINȚE (cinstit, spuse userului):**
- **Assets cartoon ABANDONATE pt look:** Quaternius low-poly (Fox/Wolf/etc din `Assets/Animals`) + Synty stilizat (`Assets/Synty`) NU se potrivesc cu realismul The Wolf. Rămân pe disc dar nu se folosesc pt produsul realist.
- **Pista web/Poki 2D MOARE** pentru viziunea asta (3D realist open-world ≠ instant-play Poki).
- **Joc mult mai mare** (3D open-world + MMORPG) decât un .io. Userul acceptă.
- **Ne trebuie assets REALISTE:** lup realist animat + pădure realistă (teren+iarbă+copaci+pietre) + HDRI/lumină + post-procesare URP. Sursă probabilă fără cont: **Poly Haven CC0** (HDRI sky, texturi PBR sol, modele roci/plante), ambientCG (texturi), Meshy (lup realist), Sketchfab CC.

**DECIS — primul pas (ales de user): „Întâi doar pădurea realistă".** Construiesc o porțiune de pădure realistă (teren + iarbă + copaci + lumină + post-procesare URP + cameră în spate) ca scenă de prezentare a atmosferei, ÎNAINTE de a pune animalul. Bara e SUS — userul s-a enervat pe „grey-box 1999"; orice screenshot trebuie să arate premium, nu placeholder.

### Clarificări + stare la finalul sesiunii 26 mai 2026
- ⚠️ The Wolf = doar REFERINȚĂ VIZUALĂ/de calitate, NU jocul literal. Facem jocul NOSTRU (lanț trofic + skill-uri + scară de lumi), dar la nivelul vizual+mișcare al The Wolf. Lupul a fost exemplu ca să-mi arate bara.
- ⚠️ PREFERINȚĂ TARE A USERULUI: vrea o **soluție AUTOMATĂ + SCALABILĂ** pentru animale (jocul are multe animale) — NU cumpărat/animat manual fiecare. Vulpea Quaternius i s-a părut „robot, nimic natural" → AI auto-rig simplu NU e suficient. „Natural" = mocap/retargeting.
- 📋 PROXIMUL PAS CONCRET (neînceput): **evaluez Anything World** (auto-rig + animații naturale pe animale, SDK Unity, bibliotecă animale + comportamente; ~75-85% natural) — testez 1 animal în Unity să vadă userul dacă mișcarea e „naturală" vs „robot", ÎNAINTE de a lega jocul de el. Alt drum dacă nu convinge: set mocap pe rig comun + retargeting pe toate animalele. Probabil cere cont/API key (ca Meshy). Mediul realist = soluție gata (ex. „Free Forest Sample" pe Asset Store, cere acquire-ul userului — cont Unity).
- ✅ BUILD-IN-PUBLIC pornit: userul a postat pe LinkedIn (EN) + Facebook (RO) povestea „dev web care se lovește de zidul Unity + Cloud PC + are un plan". Proof screenshots salvate în `C:\Users\Shadow\Pictures\LinkedIn-Proof\` (unity-editor-proof.png + character-3d-fox.png). A promis public „revin cu primul rezultat" → motivație să livrăm un „glow-up" vizual. Vrea să țină un RITM constant.
- ⚠️ Transfer fișiere Cloud PC→PC local: Claude NU poate accesa PC-ul fizic al userului; conectorii claude.ai Gmail/Drive cer /mcp auth și probabil nu atașează fișiere locale. Ce MERGE: upload prin CLI `curl.exe -F "file=@cale" https://tmpfiles.org/api/v1/upload` → link temporar (catbox „Invalid uploader", 0x0.st dezactivat). tmpfiles = link ~1h.

## ✅✅ MILESTONE 26 mai 2026 — PĂDURE REALISTĂ FUNCȚIONALĂ în Unity (userul a zis „ASTA E", „chiar te rog continuă")
**Validat că pipeline-ul realist GRATIS (Poly Haven CC0) merge end-to-end în Unity, complet autonom (fără Asset Store/cont/bani).** Scena `Game.unity` arată acum a joc real stil The Wolf: pădure de toamnă, frunze pe sol, lumină caldă, copaci reali, vulpea văzută din spate cameră în spate. De la „grey-box 1999" la cinematic într-o sesiune. Proof: `Pictures\LinkedIn-Proof\forest-glowup-after.png`.
**REȚETA (toată prin MCP, NU execute_code care e stricat):**
- Assets Poly Haven în `Assets/Forest/` (Sky/HDRI .hdr, Ground/forest_floor texturi, Models/*.gltf). gltf se importă cu glTFast 6.15.1 ca GameObject+PBR; instanțiezi cu `manage_gameobject create prefab_path=...gltf`.
- **Cer+lumină:** material `Skybox/Panoramic` (creat cu `manage_material create` + `set_material_shader_property _MainTex = calea .hdr`) → `manage_graphics skybox_set_material` → `skybox_set_ambient mode=Skybox` + `skybox_set_reflection Skybox` + `skybox_set_sun target="Directional Light"`.
- **Ceață:** `manage_graphics skybox_set_fog` (Linear, start 22 end 130, culoare caldă).
- **Soare:** rotație jos (`manage_gameobject modify rotation [38,-40,0]`) + `manage_components set_property Light {color ca OBIECT {r,g,b,a}, intensity 1.35, shadows "Soft"}`. ⚠️ culoarea TREBUIE obiect {r,g,b,a}, NU array.
- **Sol PBR:** material `Universal Render Pipeline/Lit`, `_BaseMap`=diffuse, `_BaseMap_ST`=[12,12,0,0] tiling, `_Smoothness`=0.1 → `assign_material_to_renderer` pe Ground. (normal map sărit ca să evit problema de import-type.)
- **Post-procesare:** `manage_graphics volume_create is_global effects=[Tonemapping,Bloom,ColorAdjustments,Vignette]` apoi `volume_set_effect` pe fiecare (Tonemapping mode ACES etc.). ⚠️ flag-ul camerei `renderPostProcessing` n-a putut fi setat prin MCP (componenta UniversalAdditionalCameraData „not found") — dar scena arată bine oricum din HDRI+sol+ceață.
- Camera mutată manual în spatele fox-ului (poz [3.5,1.8,-6]); food spheres dezactivate.
**RĂMÂNE (cel mai important): animalul realist.** Vulpea low-poly Quaternius se bate cap în cap cu mediul realist (arată jucărie). Următor = animal realist (eval Anything World / pachet lup realist) + mișcare naturală. ASTA e piesa care lipsește din „The Wolf look".

## 🏁 FINAL SESIUNE 26 mai 2026 — stare + țintă agreată (reluăm de AICI mâine)
- **Pădurea realistă e GATA și EXPLORABILĂ în Play mode:** teren 50×50 cu sol PBR de frunze, ~17 copaci (brazi/pini) împrăștiați, cer HDRI toamnă, lumină caldă, ceață, post-procesare (ACES+bloom+contrast+saturație, postExposure -0.55 ca să nu fie spălăcit), cameră în spatele animalului (CameraFollow offset [0,3.2,-6]). Player = Fox_Demo (cartoon, placeholder) cu AnimalPlayer (WASD + walk/idle + rotație + `runInBackground`). Scena `Game.unity` salvată. Proof în `Pictures\LinkedIn-Proof\forest-glowup-after.png`.
- **Setări iterație rapidă: NEAPLICATE încă** (userul a zis „încă nu"). De propus mâine: „Enter Play Mode without domain reload", texturi 1K în dev, etc. (explicat: lentoarea Unity = domain reload single-thread, NU hardware; Cloud PC e suficient, upgrade doar la build Android/baking/RAM-swap).
- **TIMELINE AGREAT (corectat, cinstit):** un joc care ARATĂ + se joacă ca The Wolf (single-player + boți) = **săptămâni–câteva luni** solo-cu-AI (NU ani — studioul Wolf n-avea uneltele de azi). Partea LUNGĂ/grea = **multiplayer real-time** (netcode/server autoritar) + volum de conținut + polish. Plan confirmat: **single-player+boți întâi (rapid, premium, testabil/postabil), multiplayer după.** Țintă pe care o batem în cuie: **„vertical slice jucabil pe TELEFON în ~2-4 săpt"**: 1 animal realist mișcare naturală + pădure + 1 mecanică (vânat/creștere) + cameră Wolf + sunet de bază + build Android instalabil.
- **PROXIMUL PAS (neînceput, userul a dat aproape GO):** „hartă + 2-3 animale care aleargă (boți)" ȘI animalul realist. Concret de făcut mâine: (a) animal realist + mișcare naturală (eval Anything World SAU pachet lup realist — userul vrea soluție automată/scalabilă, NU manual), (b) 2-3 animale-boți care se mișcă/fug pe hartă. Vezi [[cristian-working-style]] (el dirijează, eu fac munca) + [[animal-io-meshy]].
